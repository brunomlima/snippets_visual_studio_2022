<?xml version="1.0" encoding="utf-8"?>
<CodeSnippets xmlns="http://schemas.microsoft.com/VisualStudio/2005/CodeSnippet">
  <CodeSnippet Format="1.0.0">
    <Header>
      <Title>Save XML to File</Title>
      <Shortcut>SalvarXml</Shortcut>
      <Description>Salva um arquivo XML no diretório C:/temp.</Description>
      <Author>Bruno Lima</Author>
    </Header>
    <Snippet>
      <Code Language="csharp">
        <![CDATA[public static void SaveXmlToFile(System.Xml.Linq.XDocument xmlDocument, string fileName)
        {
            // Define o caminho base
            string directoryPath = @"C:\temp";

            try
            {
                // Verifica se o diretório existe; caso contrário, cria o diretório
                if (!System.IO.Directory.Exists(directoryPath))
                {
                    System.IO.Directory.CreateDirectory(directoryPath);
                }

                // Define o caminho completo do arquivo
                string filePath = System.IO.Path.Combine(directoryPath, fileName);

                // Salva o documento XML no arquivo
                xmlDocument.Save(filePath);

                Console.WriteLine("Arquivo XML salvo com sucesso em " + filePath);
            }
            catch (Exception ex)
            {
                Console.WriteLine("Erro ao salvar o arquivo XML: " + ex.Message);
            }
        }]]>
      </Code>
    </Snippet>
  </CodeSnippet>
</CodeSnippets>

